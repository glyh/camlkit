; A ppx driver for the test suite only: ppx_deriving and ppxlib are test
; dependencies and nothing the server or worker links. Run as `<exe> --as-ppx`,
; which is the protocol a .merlin's `FLG -ppx` names. See tickets/037.
(executable
 (name demo_ppx)
 (modes byte_complete)
 (libraries ppxlib ppx_deriving.show))
